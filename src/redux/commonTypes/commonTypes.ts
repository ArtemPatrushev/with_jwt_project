export type UserType = {
  email: string,
  id: string,
  refreshToken: string,
  roles: [],
  token: string,
  userName: string,
  isAuth: boolean
};

export type ErrorsFormType = {
  email: string
};
